import list from "./container";

export default function stChild({scheme, stream}) {

    scheme.

}



const schema =

    ["name",
        ["name", "string", { len: 5, def: "1.0.0" }],
        ["name", "string", { len: 5, def: "1.0.0" }]
    ]

;